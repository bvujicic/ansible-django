from prostoria.settings import *

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True
# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'abcdefghijkl'

INTERNAL_IPS = ['127.0.0.1']
ALLOWED_HOSTS = []

INSTALLED_APPS += [
    'debug_toolbar'
]

MIDDLEWARE_CLASSES += [
    'debug_toolbar.middleware.DebugToolbarMiddleware'
]

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '{{ db.name }}',
        'USER': '{{ db.owner }}',
        'PASSWORD': '{{ db.password }}',
        'HOST': 'localhost',
        'PORT': '5432'
    }
}

HOST_MIDDLEWARE_URLCONF_MAP = {
    'example.com': 'prostoria.urls',
    'subdomain.example.com': 'market.urls'
}

HOST_MIDDLEWARE_LANGUAGE_MAP = {
    'example.com': ('hr', 'en', 'de', 'fr'),
    'subdomain.example.com': ('hr', 'sr'),
}

# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/1.10/howto/static-files/

STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'resources/static')
]
STATIC_URL = '/static/'
STATIC_ROOT = '/path/to/somewhere'
