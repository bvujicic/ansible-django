---

- name: Create "{{ host.user }}" user
  user:
    name: "{{ host.user }}"
    password: "{{ password }}"
    shell: /bin/bash
    groups: sudo
    append: true
    #generate_ssh_key: true
    state: present